{
  "name": "master-numerics",
  "version": "1.0.0",
  "private": true,
  "description": "Master Numerics — immersive math & physics education platform (monorepo: client + server).",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "cd server && npm install --include=dev && npx prisma generate && npm run build && cd ../client && npm install --include=dev && npm run build",
    "start": "cd server && npm run start:prod"
  }
}
